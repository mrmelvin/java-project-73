server:
  port: ${PORT:5000}

base-url: /api

ROLLBAR_TOKEN: c35649e10a3e4db4af846ae8d5df1666

spring:
  application:
    name: app

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop